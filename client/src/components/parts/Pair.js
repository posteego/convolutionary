import React from "react";
import { ListItem } from "./ListItem";

const Pair = ({ title, subtitle, authors, link, description, image, Button }) => (
  <ListItem>
    <div>

    </div>
  </ListItem>
);

export default Pair;
